# ===============================
# Configuracao do Banco de Dados DOCKER
# ===============================
spring.datasource.url=jdbc:mysql://reusemi-db:3306/reusemi_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=REUSEMI123

# ===============================
# Configuracoes Especoeficas do Docker
# ===============================
# Ajuste paths para dentro do container
spring.web.resources.static-locations=classpath:/static/,file:/app/uploads/
logging.file.name=/app/logs/reusemi.log

# O resto herda do application.properties